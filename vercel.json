{
    "framework": "nextjs",
    "installCommand": "pnpm install --no-frozen-lockfile",
    "buildCommand": "pnpm --filter @brokerbox/database db:generate && pnpm --filter @brokerbox/web build && mkdir -p .next && cp -r apps/web/.next/. .next/"
}
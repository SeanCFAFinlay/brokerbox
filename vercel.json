{
    "version": 2,
    "buildCommand": "pnpm -C apps/broker vercel-build",
    "outputDirectory": "apps/broker/dist",
    "framework": null,
    "installCommand": "pnpm install",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}
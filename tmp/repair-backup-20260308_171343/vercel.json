{
    "buildCommand": "pnpm run build --filter @brokerbox/web && mkdir -p .next && cp -r apps/web/.next/. .next/",
    "installCommand": "pnpm install --no-frozen-lockfile",
    "framework": "nextjs"
}
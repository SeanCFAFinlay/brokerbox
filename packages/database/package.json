{
  "name": "@brokerbox/database",
  "version": "1.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "pnpm exec prisma generate --schema=./prisma/schema.prisma && tsc",
    "dev": "tsc -w",
    "db:generate": "pnpm exec prisma generate --schema=./prisma/schema.prisma",
    "db:push": "pnpm exec prisma db push --schema=./prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "typescript": "^5.3.3"
  }
}
/**
 * Re-export from canonical @brokerbox/domain match engine.
 * Prefer importing from '@brokerbox/domain' in new code.
 */
export {
  runMatch,
  type BorrowerData,
  type DealData,
  type LenderData,
  type MatchResultItem as MatchResult,
} from '@brokerbox/domain';
